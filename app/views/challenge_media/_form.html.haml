= form_for @challenge_medium do |f|
  - if @challenge_medium.errors.any?
    #error_explanation
      %h2= "#{pluralize(@challenge_medium.errors.count, "error")} prohibited this challenge_medium from being saved:"
      %ul
        - @challenge_medium.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :content_url
    = f.text_field :content_url
  .field
    = f.label :string
    = f.text_field :string
  .field
    = f.label :content_type
    = f.text_field :content_type
  .field
    = f.label :string
    = f.text_field :string
  .actions
    = f.submit 'Save'
