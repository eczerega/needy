= form_for @commnet_like do |f|
  - if @commnet_like.errors.any?
    #error_explanation
      %h2= "#{pluralize(@commnet_like.errors.count, "error")} prohibited this commnet_like from being saved:"
      %ul
        - @commnet_like.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :commnets_id
    = f.text_field :commnets_id
  .field
    = f.label :integer
    = f.text_field :integer
  .field
    = f.label :value
    = f.text_field :value
  .field
    = f.label :integer
    = f.text_field :integer
  .field
    = f.label :user_id
    = f.text_field :user_id
  .field
    = f.label :integer
    = f.text_field :integer
  .actions
    = f.submit 'Save'
