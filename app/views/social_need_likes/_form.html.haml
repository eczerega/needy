= form_for @social_need_like do |f|
  - if @social_need_like.errors.any?
    #error_explanation
      %h2= "#{pluralize(@social_need_like.errors.count, "error")} prohibited this social_need_like from being saved:"
      %ul
        - @social_need_like.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :user_id
    = f.text_field :user_id
  .field
    = f.label :integer
    = f.text_field :integer
  .field
    = f.label :social_need_id
    = f.text_field :social_need_id
  .field
    = f.label :integer
    = f.text_field :integer
  .field
    = f.label :value
    = f.text_field :value
  .field
    = f.label :integer
    = f.text_field :integer
  .actions
    = f.submit 'Save'
