= form_for @business_need_category do |f|
  - if @business_need_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@business_need_category.errors.count, "error")} prohibited this business_need_category from being saved:"
      %ul
        - @business_need_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :category_id
    = f.text_field :category_id
  .field
    = f.label :integer
    = f.text_field :integer
  .field
    = f.label :social_need_id
    = f.text_field :social_need_id
  .field
    = f.label :integer
    = f.text_field :integer
  .actions
    = f.submit 'Save'
