%h1 Listing sponsors

%table
  %tr
    %th Name
    %th String
    %th Picture
    %th String
    %th Web
    %th String
    %th
    %th
    %th

  - @sponsors.each do |sponsor|
    %tr
      %td= sponsor.name
      %td= sponsor.string
      %td= sponsor.picture
      %td= sponsor.string
      %td= sponsor.web
      %td= sponsor.string
      %td= link_to 'Show', sponsor
      %td= link_to 'Edit', edit_sponsor_path(sponsor)
      %td= link_to 'Destroy', sponsor, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Sponsor', new_sponsor_path
